# ===========================================
# DNI Lookup API - Variables de Entorno
# ===========================================
# Copiar este archivo como .env y configurar los valores

# Base de datos SQLite (persistente en /app/data)
DATABASE_URL=sqlite:///./data/personas.db

# Credenciales de administrador (para el panel web)
# IMPORTANTE: Cambiar en producción
ADMIN_USER=admin
ADMIN_PASSWORD=tu_password_seguro

# Credenciales para descargar backup de la base de datos
# IMPORTANTE: Usar credenciales fuertes y únicas
DB_BACKUP_USER=backup_admin
DB_BACKUP_PASSWORD=tu_password_backup_seguro

# Token de apisperu.com (opcional, también se puede configurar desde la UI)
# Obtener en: https://apisperu.com
APISPERU_TOKEN=

# Rate Limiting (peticiones por minuto por IP)
# Default: 200 peticiones por minuto por IP
RATE_LIMIT_PER_IP=200
